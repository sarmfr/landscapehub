kind: wasmer.io/App.v0
owner: samson
name: landscapehub
package: .

debug: false

scaling:
  mode: single_concurrency

locality:
  regions:
    - us-socal1

volumes:
  - name: media
    mount: /app/storage/app/public
